{% extends "layout.peb" %}

{% block title %}Paramètres - UGE Webmail{% endblock %}

{% block content %}
<div class="mail-layout">
    <!-- Sidebar -->
    <aside class="sidebar">
        <a href="/mail/inbox" class="btn btn-secondary btn-compose">← Retour</a>
        
        <nav class="folder-nav">
            <a href="/mail/inbox" class="folder-link">📥 Boîte de réception</a>
            <a href="/mail/outbox" class="folder-link">📤 Messages envoyés</a>
            <a href="/mail/drafts" class="folder-link">📝 Brouillons</a>
            <a href="/mail/trash" class="folder-link">🗑️ Corbeille</a>
        </nav>
    </aside>
    
    <!-- Main content -->
    <div class="mail-content">
        <h1>⚙️ Paramètres</h1>
        
        {% if error is not empty %}
        <div class="alert alert-error">{{ error }}</div>
        {% endif %}
        
        {% if success is not empty %}
        <div class="alert alert-success">{{ success }}</div>
        {% endif %}
        
        <div class="settings-section">
            <h2>👤 Informations du compte</h2>
            <p><strong>Nom d'utilisateur :</strong> {{ username }}</p>
        </div>
        
        <div class="settings-section">
            <h2>🔐 Changer le mot de passe</h2>
            
            <form method="post" action="/mail/settings/password" class="settings-form">
                <div class="form-group">
                    <label for="oldPassword">Mot de passe actuel</label>
                    <input type="password" id="oldPassword" name="oldPassword" required>
                </div>
                
                <div class="form-group">
                    <label for="newPassword">Nouveau mot de passe</label>
                    <input type="password" id="newPassword" name="newPassword" required minlength="8">
                    <small>Minimum 8 caractères avec majuscule, minuscule, chiffre et caractère spécial</small>
                </div>
                
                <div class="form-group">
                    <label for="confirmPassword">Confirmer le nouveau mot de passe</label>
                    <input type="password" id="confirmPassword" name="confirmPassword" required>
                </div>
                
                <button type="submit" class="btn btn-primary">Changer le mot de passe</button>
            </form>
        </div>
        
        <div class="settings-section">
            <h2>ℹ️ Informations</h2>
            <ul>
                <li>Les messages dans la corbeille sont automatiquement supprimés après 30 jours.</li>
                <li>Les pièces jointes sont dédupliquées pour économiser l'espace disque.</li>
                <li>Vous pouvez recevoir des messages anonymes via UDP sur le port 9999.</li>
            </ul>
        </div>
    </div>
</div>
{% endblock %}
